name: inventory_app
description: A new Flutter project converting the inventory HTML app to mobile.
publish_to: 'none'
version: 1.0.1+2
environment:
  sdk: '>=3.0.0 <4.0.0'
dependencies:
  flutter:
    sdk: flutter
  webview_flutter: ^4.10.0
  webview_flutter_android: ^4.10.15
  webview_flutter_web: ^0.2.3+4
  webview_flutter_wkwebview: ^3.24.2
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
flutter:
  uses-material-design: true
  assets:
    - assets/index.html
